// RPG battles
// Corey Klassen

function setup() {
  createCanvas(windowWidth, windowHeight);
}

function draw() {
  background(220);
}

function keyPressed() {
  if (key === "a") {
    rect(50, 50, 50, 50,);
    fill("green");  
  }
}
